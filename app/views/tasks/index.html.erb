<ul>
  <% @tasks.each do |task| %>
    <% complete = task.completed ? "Done" : "Not done" %>
    <li><%= complete %><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
